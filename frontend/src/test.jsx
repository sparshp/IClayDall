import React from 'react'

const test = () => {
  return (
    {
        products.map((product) => (
    //   { product &&  return <b>{product.name}</b>}
        }
        ))
        }
}

export default test